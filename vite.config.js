import { defineConfig } from 'vite'
import react from '@vitejs/react-web'

export default defineConfig({
  plugins: [react()],
  base: '/Vigneshwara-Furnitures/', // Replace this with your EXACT repo name
})